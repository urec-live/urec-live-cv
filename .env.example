# Environment variables for urec-live-cv

# Port to run the API on (Railway sets this automatically)
PORT=8000

# Environment mode (production or development)
# Set to development to enable auto-reload (not recommended for production)
ENVIRONMENT=production
